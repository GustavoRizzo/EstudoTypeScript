console.log('Rodando index.js!');

/*// Parte3: criando API
const expressAPI = require('express');
const appAPI = expressAPI();
const bodyParserAPI = require('body-parser');
const portAPI = 3001;
const mysqlAPI = require('mysql');

// - Configurando o body parser para receber POST mais tadre
appAPI.use(bodyParserAPI.urlencoded({extended: true}));
appAPI.use(bodyParserAPI.json());

// - definando as rotas
const rounterAPI = express.Router();
rounterAPI.get('/', (req, res) => res.json({menssage: 'Funcionando!'}));
app.use('/', rounterAPI);

// - inicia o servidor
appAPI.listen(port);
console.log('API funcionando!');*/